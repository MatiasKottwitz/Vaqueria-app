import React, {createContext} from "react";
//Se declara un context global para usarlo en lod distintos componentes.
export const DataContext = createContext(null);
export const DatoContext = createContext(null);